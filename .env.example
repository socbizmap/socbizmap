# Copy to `.env` locally. Never commit real keys.
# Without both values the app uses the in-memory/AsyncStorage mock.

# Project host: mutfwhenuegvdwhgwnty.supabase.co
EXPO_PUBLIC_SUPABASE_URL=https://mutfwhenuegvdwhgwnty.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Show email OTP / magic-link on login (also auto-shown if SMS send fails).
# Phone SMS needs Twilio Trust Hub KYC; email uses Supabase Auth templates.
EXPO_PUBLIC_AUTH_EMAIL=
